<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/wheelFragment">

    <fragment
        android:id="@+id/wheelFragment"
        android:name="com.elifnuroksuz.quiz.WheelFragment"
        android:label="fragment_wheel"
        tools:layout="@layout/fragment_wheel" >
        <action
            android:id="@+id/action_wheelFragment_to_questionFragment"
            app:destination="@id/questionFragment" />
    </fragment>
    <fragment
        android:id="@+id/questionFragment"
        android:name="com.elifnuroksuz.quiz.QuestionFragment"
        android:label="fragment_question"
        tools:layout="@layout/fragment_question" />
</navigation>